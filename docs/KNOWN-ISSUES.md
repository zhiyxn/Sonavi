# P11 已知问题

日期：2026-09-16

本文件记录修复复核后仍开放的问题。用户原始 Windows 体验记录保留在 `docs/Existing issues.md`；“代码已修复”不等于真实服务器或另一平台已经验证。

## Blocker（0）

暂无。

## Critical（0）

原 RC-C-001 已修复：媒体句柄改为每次会话密钥加密、带会话 epoch 的无状态不透明 token，不再由 2,000 项全局 FIFO 淘汰。单元测试确认先创建队列音频、再创建 10,000 个封面句柄后音频仍可解析；断开、忘记账号和退出仍会整体失效旧句柄。

## Major（4）

### RC-MA-001：真实大型艺术家索引仍待复验

- `getArtists` 已从统一 1 MiB 上限分离为有界 16 MiB 上限，其他 API 仍保持 1 MiB；用户报告后的后续播放失败也由无状态媒体句柄修复覆盖。
- fixture、单元测试和 Windows 打包冒烟通过，但尚未在发生过错误的真实 Navidrome 资料库复验，因此状态是“修复待真服验证”，不能宣称真实问题已关闭。

### RC-MA-002：真实服务器 scrobble 计数仍待复验

- 除累计播放阈值外，真实 `ended` 状态现在也会触发一次 submission；queueEntryId 去重仍防止重复上报。
- 自动化已观察 now-playing 与 submission，但真实服务器版本、响应和计数口径尚未取得，仍不能标为通过。

### RC-MA-003：网络中断只具备手动恢复

- 播放错误现在提供“重试播放”：原始流按当前进度重建，支持 `transcodeOffset` 的转码流按当前时间请求新句柄。
- 尚未实现在线事件自动恢复与退避策略；断网后是否能在不同服务器/代理环境恢复仍需真机验证。

### RC-MA-004：真实音频格式矩阵尚未建立

- 当前真实 `HTMLAudioElement` 自动化只解码合成 PCM WAV。
- MP3、AAC/M4A、FLAC、Opus/Ogg 和真实 Navidrome MP3 转码输出在 Windows、macOS Intel、macOS arm64 均需实际样本；不能由扩展名或 MIME 白名单推断通过。

## Minor（0）

已修复：未连接侧栏假按钮、播放器状态宽度漂移、Windows 宽滚动条、侧栏/播放器不跟随主题、专辑详情错误空白、生产 CSP 保留 HMR WebSocket、诊断把 12 秒内部超时报成 `cancelled`、同一媒体请求同时记录 `cancelled` 与 `broken-stream`、诊断缺少端点名与错误文本。

## 诊断与网络策略待决（2026-09-16）

- API 超时仍是固定 12 秒：`getArtists` 已放宽响应上限到 16 MiB，但超时没有同步放宽；真实服务器上仍出现 12 秒无响应记录，需要直连与代理对照后再决定是否按端点区分超时。
- renderer 用 TanStack Vue Query 默认 `retry: 3`，单个查询失败最多产生 4 次超时尝试；诊断条目没有尝试序号，无法区分“一个查询重试多次”与“多次用户操作”，要分组必须从 renderer 传递请求标识。
- 媒体消费者取消时只记录终态，没有主动 `abort()` 上游 fetch；被取消的下载会等上游自然结束。

## Enhancement（0）

已完成：账号忘记入口仅保留在设置、搜索结果滚动自动分页、设置使用项目自有 Reka/shadcn 风格 Select、托盘退出文案简化为“退出 Sonavi”；专辑滚动自动分页已在 P12-MI-016 进一步替换为显式 Pagination。

## 本轮已验证的修复

- `lint`、`typecheck`、24 个文件/115 项 Vitest、生产构建和源码 Electron 冒烟通过。
- Windows x64 未签名 NSIS 构建、包验证和打包应用完整冒烟通过。
- 生产 renderer CSP 为 `connect-src 'self'`，不含 `ws://localhost:*`；开发环境仍保留 HMR 所需来源。
- Windows 包 SHA-256：`72a5be09328afe0cceff365e85414880acd19e06c32cbae1265abebe0c5b9aac`。

未验证项目以 `docs/TEST-MATRIX.md` 为准；本轮没有配置签名、公证、自动更新或发布 Release。

## P12 真机回归问题记录

P12 起点：`fe5b07a`。本节只记录本轮在真实 Windows/macOS、真实 Navidrome 和真实音乐库中观察到的问题；不会把历史 fixture/CI 失败重复计算为 P12 真机缺陷。

每个新问题必须使用以下字段：

- 编号与标题；
- 状态：`OPEN` / `RETEST` / `CLOSED`；
- 平台、应用来源与架构；
- 前置条件与复现步骤；
- 实际结果与预期结果；
- 脱敏日志或截图位置；
- 影响范围；
- 对应 `docs/TEST-MATRIX.md` 用例 ID。

现有 RC-MA-001～RC-MA-004 继续开放，只有对应 P12 真实用例取得 `PASS` 后才能关闭。

### P12-MI-001：打开播放队列没有定位当前歌曲

- 状态：`RETEST`。
- 平台：Windows 11 x64；`fe5b07a` 构建的未签名 `0.1.0-rc.4` 测试包。
- 前置条件：真实服务器已连接，队列包含多首歌曲，当前播放项不是第一项。
- 复现步骤：播放队列中靠后的歌曲；关闭播放队列；重新打开播放队列。
- 实际结果：列表每次从第一项显示，当前歌曲可能在视口外。
- 预期结果：每次打开队列时，当前歌曲自动进入可视区域。
- 日志：无错误日志；属于可稳定复现的 UI 定位问题。
- 影响范围：长队列中难以确认当前播放位置；不改变播放、队列顺序或音频流。
- 对应用例：C-14。
- 修复：队列打开并完成 DOM 渲染后，将唯一 `aria-current="true"` 项滚动到列表中央；已增加组件回归测试。2026-09-17 新 Windows x64 未签名包已构建并通过包验证，等待真机复验后关闭。

### P12-MI-002：播放队列当前歌曲标题对比不足

- 状态：`RETEST`。
- 平台：Windows 11 x64；未签名 `0.1.0-rc.4` 测试包。
- 前置条件：播放队列已打开且存在当前歌曲。
- 复现步骤：播放任意队列歌曲；打开播放队列；观察当前歌曲标题。
- 实际结果：浅色主题中标题使用浅色 `accent-subtle`，深色主题截图中标题同样与当前行背景接近，歌曲名难以辨认。
- 预期结果：当前歌曲在浅色和深色主题下标题均清晰可读，当前行仍可稳定辨识。
- 日志：无错误日志；由用户提供的 Windows 浅色与深色真机截图确认，属于视觉对比问题。
- 影响范围：只影响播放队列当前项的辨识，不影响歌曲、播放状态或队列操作。
- 对应用例：C-15。
- 修复：标题改用各主题的高对比正文色 `--sonavi-chrome-text`，主强调色只用于 3px 左侧当前行标记；等待本批问题统一完成并生成新包后真机复验。

### P12-MI-003：主播放/暂停按钮图标对比不足

- 状态：`RETEST`。
- 平台：Windows 11 x64；未签名 `0.1.0-rc.4` 测试包。
- 前置条件：存在当前歌曲，播放器底栏显示播放或暂停主控制。
- 复现步骤：开始播放任意歌曲；观察中间的播放/暂停按钮；将鼠标移入按钮再次观察。
- 实际结果：播放器区域的通用按钮规则覆盖默认按钮的白色前景和悬停背景，棕色图标落在琥珀色背景上，难以辨认。
- 预期结果：播放与暂停图标在正常和悬停状态下均清晰可读。
- 日志：无错误日志；由用户提供的 Windows 真机截图确认，属于 CSS 优先级问题。
- 影响范围：只影响主播放控制的视觉辨识，不影响实际播放或暂停行为。
- 对应用例：C-16。
- 修复：为主播放控制增加独立样式标记，正常与悬停状态均固定使用白色图标及对应强调色背景；等待本批问题统一完成并生成新包后真机复验。

### P12-MI-004：点击播放器其他区域不会关闭播放队列

- 状态：`RETEST`。
- 平台：Windows 11 x64；未签名 `0.1.0-rc.4` 测试包。
- 前置条件：播放队列已打开。
- 复现步骤：打开播放队列；点击播放器底栏中队列面板之外的歌曲信息、封面或播放控制区域。
- 实际结果：播放队列继续保持展开。
- 预期结果：点击播放器其他区域时关闭播放队列；队列内部交互和队列按钮自身不被误判。
- 日志：无错误日志；属于播放器局部交互问题。
- 影响范围：队列面板需要再次点击队列按钮才能收起，不影响播放和队列数据。
- 对应用例：C-17。
- 修复：播放器底栏处理冒泡点击；队列面板内部及队列切换按钮保持原行为，其余区域关闭队列且不阻断原点击操作。等待本批问题统一完成后真机复验。

### P12-MI-005：页面栏目标题显示无意义的重复序号

- 状态：`RETEST`。
- 平台：Windows 11 x64；代码层面同时影响 Windows 与 macOS 共享 renderer。
- 前置条件：进入连接、音乐库、艺术家、搜索、收藏、歌单或设置页面。
- 复现步骤：依次切换页面；观察页面标题上方的英文栏目眉题。
- 实际结果：栏目名称前显示硬编码的 `01 /`、`03 /`、`05 /`、`06 /`，其中 05 和 06 重复且没有导航或业务含义。
- 预期结果：栏目眉题只显示栏目名称，不呈现误导性的伪序号。
- 日志：无错误日志；属于共享 renderer 文案一致性问题。
- 影响范围：所有目标平台的七个页面眉题；不影响导航或页面功能。
- 对应用例：G-C01。
- 修复：统一移除七个页面眉题中的数字与斜线，只保留 CONNECT、LIBRARY、ARTISTS、SEARCH、FAVORITES、PLAYLISTS、SETTINGS；等待本批问题统一完成后真机复验。

### P12-MI-006：完整艺术家索引请求慢且缓存时间过短

- 状态：`RETEST`。
- 平台：Windows 11 x64；真实 Navidrome 服务器与真实音乐库。
- 前置条件：已连接真实服务器，音乐库包含足以使 `getArtists` 响应明显变慢的艺术家数据。
- 复现步骤：进入艺术家页面等待完整列表；离开页面；原缓存超过 30 秒后再次进入或重新聚焦应用。
- 实际结果：公共 `getArtists` 一次返回完整索引，首次请求较慢；原成功结果只保持 30 秒新鲜度，可能再次发起同一全量请求。
- 预期结果：同一连接会话复用已成功取得的完整索引，避免切页或窗口重新聚焦造成重复慢请求。
- 日志：没有记录服务器地址或凭据；代码链路确认单次响应上限 16 MiB、超时 12 秒。用户真机反馈接口较慢，尚待新包记录脱敏耗时。
- 影响范围：大型音乐库的艺术家页面进入速度和服务端负载；艺术家详情仍按需单独请求。
- 对应用例：B-03。
- 修复：成功结果在当前连接会话内保持新鲜并常驻查询缓存，禁用窗口聚焦重取；断开连接、退出账号或网络配置变化仍由既有 `queryClient.clear()` 清除。首次全量请求受公共协议限制，不冒充已优化。

### P12-MI-007：可点击按钮未统一显示小手光标

- 状态：`RETEST`。
- 平台：Windows 11 x64；代码层面同时影响 Windows 与 macOS 共享 renderer。
- 前置条件：页面存在可用或禁用按钮。
- 复现步骤：将鼠标依次移入导航、播放控制、页面操作等按钮。
- 实际结果：部分可点击按钮仍使用默认箭头光标，交互提示不一致。
- 预期结果：所有可用按钮显示 `pointer`；禁用按钮显示 `not-allowed`，连接请求中的等待按钮继续由更具体规则显示 `wait`。
- 日志：无错误日志；属于共享 renderer 样式一致性问题。
- 影响范围：所有原生 `button` 元素；不改变按钮行为、键盘操作或可访问名称。
- 对应用例：G-C02。
- 修复：在全局基础样式中为可用和禁用按钮分别增加光标规则，并通过真实 CSS 计算样式测试验证；等待本批问题统一完成后真机复验。

### P12-MI-008：真实服务器播放记录上报失败

- 状态：`OPEN`。
- 平台：Windows 11 x64；真实 Navidrome 服务器与真实音乐库。
- 前置条件：已连接真实服务器并开始播放真实歌曲。
- 复现步骤：播放歌曲；等待播放器尝试 now-playing 或达到完成上报阈值；观察播放器歌曲信息区域。
- 实际结果：界面显示“播放记录暂时未同步；音乐播放不受影响。”，表示至少一次 `scrobble` 请求返回失败或网络异常。
- 预期结果：now-playing 和达到阈值后的 submission 均成功，界面不显示同步失败提示，服务器播放计数按其规则更新。
- 日志：尚未取得设置页中 operation 为 `scrobble` 的脱敏诊断；不得记录服务器地址、账号、token、salt、Cookie 或 Authorization。
- 影响范围：服务器播放记录、最近播放或统计计数可能缺失；音频播放不受影响。
- 对应用例：C-18；同时为既有 RC-MA-002 的真实服务器失败证据。
- 当前判断：功能已实现，提示不是占位文案。当前每个队列项的 now-playing 和 submission 各只尝试一次；失败不盲目重试，以避免未知结果下重复计数。需要脱敏诊断中的状态、错误分类和耗时定位服务端、网络或兼容性原因。

### P12-MI-009：播放栏未显示当前歌曲封面

- 状态：`RETEST`。
- 平台：Windows 11 x64；代码层面同时影响 Windows 与 macOS 共享 renderer。
- 前置条件：当前歌曲带有服务器封面。
- 复现步骤：播放带封面的歌曲；观察播放器底栏左侧图块。
- 实际结果：播放器始终显示音乐符号占位，不显示当前歌曲封面。
- 预期结果：存在封面时显示当前歌曲封面；无封面或未选歌时使用安全占位符。
- 日志：无错误日志；属于现有封面数据未在播放器使用的 UI 问题。
- 影响范围：播放器底栏视觉识别；不改变封面请求权限、播放或队列行为。
- 对应用例：C-19。
- 修复：播放器复用当前 `TrackSummary.coverUrl` 的受限媒体句柄显示封面，不创建新接口或暴露上游地址；无封面回退保持不变，等待本批问题统一完成后真机复验。

### P12-MI-010：专辑详情返回后丢失列表滚动位置

- 状态：`RETEST`。
- 平台：Windows 11 x64；代码层面同时影响 Windows 与 macOS 共享 renderer。
- 前置条件：专辑列表足够长，可以向下滚动并打开某张专辑。
- 复现步骤：在专辑列表向下滚动；打开一张专辑；点击“返回专辑”。
- 实际结果：返回后列表滚动位置回到顶部，需要重新寻找原专辑。
- 预期结果：返回后恢复进入详情前的列表滚动位置和已加载分页数据。
- 日志：无错误日志；属于共享页面状态恢复问题。
- 影响范围：首页最近添加与全部专辑列表；不改变查询分页、专辑详情或播放器。
- 对应用例：C-20。
- 修复：仅在从当前专辑列表打开详情时记录 `.workspace` 的 `scrollTop`，详情置顶显示，返回后在 DOM 更新完成时恢复原位置；从搜索等外部入口进入详情不会错误恢复旧位置。当前已启动的测试包不含该修复，等待下一统一包真机复验。

### P12-MI-011：切换栏目后丢失各页面滚动位置

- 状态：`RETEST`。
- 平台：Windows 11 x64；代码层面同时影响 Windows 与 macOS 共享 renderer。
- 前置条件：专辑、首页或其他页面内容足够长，可以产生页面滚动。
- 复现步骤：在专辑等页面向下滚动；切换到另一个栏目并滚动；再分别返回之前的页面。
- 实际结果：返回页面后滚动位置重置，无法继续浏览离开前的位置。
- 预期结果：首页、专辑、艺术家、搜索、收藏、歌单及详情分别恢复离开时的滚动位置，不互相覆盖；设置页每次从顶部开始；退出或更换账号后不继承旧会话位置。
- 日志：无错误日志；属于共享应用外壳的页面状态恢复问题。
- 影响范围：首页、专辑、艺术家、搜索、收藏、歌单和详情长页面；设置页明确排除，不改变服务器查询、分页或播放状态。
- 对应用例：F-10。
- 修复：应用外壳按页面以及专辑/艺术家详情键保存 `.workspace` 的 `scrollTop`，艺术家内部虚拟列表单独保存位置，切换完成并等待 DOM 更新后恢复；设置页不写入缓存且每次进入置顶。成功连接、断开连接和忘记账号时清空记录。当前运行包不含该修复，等待下一统一包真机复验。

### P12-MI-012：艺术家详情中的专辑丢失来源导航上下文

- 状态：`RETEST`。
- 平台：Windows 11 x64；代码层面同时影响 Windows 与 macOS 共享 renderer。
- 前置条件：搜索结果中存在带专辑的艺术家。
- 复现步骤：搜索艺术家；打开艺术家详情；打开该艺术家的一张专辑；观察左侧菜单；在专辑详情点击返回。
- 实际结果：打开专辑后左侧错误激活“专辑”；返回进入全部专辑列表，原艺术家详情和浏览位置丢失。
- 预期结果：艺术家详情进入专辑后左侧继续激活“艺术家”；返回原艺术家详情并恢复离开前的位置。
- 日志：无错误日志；属于共享应用外壳的导航来源状态丢失。
- 影响范围：搜索 → 艺术家详情 → 专辑详情路径；从首页或专辑列表进入专辑的原有返回行为不变。
- 对应用例：F-11。
- 修复：艺术家详情使用独立专辑打开路径，保留 `activeView` 与艺术家 ID；专辑返回按钮改为“返回艺术家详情”，返回后恢复艺术家详情滚动位置。艺术家来源的专辑详情不再额外请求完整专辑列表。当前已启动的测试包不含该修复，等待下一统一包真机复验。

### P12-MI-013：专辑详情滚动了整个页面而不是歌曲列表

- 状态：`RETEST`。
- 平台：Windows 11 x64；代码层面同时影响 Windows 与 macOS 共享 renderer。
- 前置条件：打开包含足够多歌曲、能产生纵向滚动的专辑详情。
- 复现步骤：进入专辑详情；在歌曲区域向下滚动；观察标题、返回按钮、专辑封面和歌曲列表的位置。
- 实际结果：整个详情页面随歌曲一起滚动，专辑标题、操作和封面离开可视区域。
- 预期结果：详情页面外层固定，仅歌曲列表在剩余空间内独立滚动。
- 日志：无错误日志；属于共享 renderer 布局和滚动容器问题。
- 影响范围：所有专辑详情入口；不改变歌曲顺序、播放、收藏或服务器请求。
- 对应用例：B-11。
- 修复：专辑详情状态下关闭 workspace 外层滚动，详情布局占满可用高度并固定标题、操作和专辑摘要；歌曲列表使用独立纵向滚动、滚动边界隔离和键盘焦点。专辑列表及其他页面仍使用原 workspace 滚动。当前已启动的测试包不含该修复，等待下一统一包真机复验。

### P12-MI-014：搜索输入过程会自动发起请求

- 现象：搜索框输入后等待约 300ms 会自动搜索，不符合本轮真机测试期望的显式提交方式。
- 影响范围：搜索页请求触发方式；不改变 `search3` 协议、结果分页、过期请求取消或播放入口。
- 修复：输入只更新草稿关键词；按 Enter 或点击搜索按钮时才提交去除首尾空白后的关键词，空白输入不可提交。
- 当前状态：`RETEST`。定向测试与全量自动检查通过；按用户要求未重新构建或启动应用，当前运行包不包含该修复。
- 对应用例：B-04。

### P12-MI-015：艺术家列表出现内外两条滚动条

- 状态：`RETEST`。
- 平台：Windows 11 x64；代码层面同时影响 Windows 与 macOS 共享 renderer。
- 现象：艺术家虚拟列表已有独立滚动条，列表标题、间距和固定 480px 高度又使外层 workspace 产生第二条滚动条。
- 影响范围：艺术家列表页；艺术家详情、专辑详情和其他栏目保持各自原有滚动策略。
- 修复：艺术家列表态关闭 workspace 外层滚动，页面改为占满可用高度的纵向布局；虚拟列表填充标题下方剩余空间，并用 ResizeObserver 按实际高度更新可见窗口。列表增加键盘焦点和可访问名称。
- 当前状态：定向测试与全量自动检查通过；未重新构建或启动应用，当前运行包不包含该修复。
- 对应用例：B-03。

### P12-MI-016：首页与专辑改为显式分页控制

- 状态：`RETEST`。
- 平台：Windows 11 x64；代码层面同时影响 Windows 与 macOS 共享 renderer。
- 现象：首页和专辑此前依赖滚动到底部后自动请求下一批，不符合本轮要求的明确分页操作。
- 预期结果：两页都使用 shadcn-vue Pagination，提供页码、上一页和下一页；首页与专辑分别保留当前页，进入详情再返回不丢失页码，翻页后回到内容顶部。
- 实现：`LibraryPanel` 改为按 session、列表类型和页码查询单页 30 张；公共协议不返回总数，因此只显示已知页和由 `hasMore` 确认的下一页。组件源码由官方 CLI 加入项目并按既有依赖、中文文案和严格 TypeScript 规则适配。
- 自动验证：Node.js 22.21.1 下首页 `newest` 与专辑 `alphabeticalByName` 参数化覆盖 `offset 0 → 30`、末页禁用和翻页置顶；应用外壳测试覆盖两个栏目页码互不覆盖。`npm run lint`、`npm run typecheck` 与 26 文件/139 项全量测试通过。
- 当前状态：未重新构建或启动应用，当前运行包不包含该改动。

### P12-MI-017：错误提醒与二次确认未使用统一组件

- 状态：`RETEST`。
- 平台：Windows 11 x64；代码层面同时影响 Windows 与 macOS 共享 renderer。
- 现象：错误提示分散为页面内文字，删除歌单和退出并忘记账号使用系统原生 `window.confirm`，样式及跨平台体验不一致。
- 预期结果：错误提醒使用 shadcn-vue Sonner；危险操作必须使用具有明确确认/取消语义、焦点管理和模态边界的统一组件。
- 修复：根节点挂载唯一 Toaster；连接、设置、收藏/歌单、音乐库/艺术家/搜索/歌词查询、播放和 scrobble 错误走统一 `toast.error`。阻断当前页面的加载错误仍保留重试卡。P12-MI-019 后，删除歌单和忘记账号由 shadcn-vue AlertDialog 承担确认，Sonner 只保留通知职责。
- 自动验证：Node.js 22.21.1 下通知工具、确认/取消解析、歌单删除门控和忘记账号实际 Sonner 交互均有回归；`npm run lint`、`npm run typecheck` 与 28 文件/144 项全量测试通过。
- 当前状态：Sonner 错误通知已由 P12-MI-019 的 Windows 源码 Electron 冒烟覆盖；危险确认实现已改为 AlertDialog。安装包仍等待统一生成与真机复验。

### P12-MI-018：切换栏目会自动重取整页数据

- 状态：`RETEST`。
- 平台：Windows 11 x64；代码层面同时影响 Windows 与 macOS 共享 renderer。
- 现象：数据页组件切换后会销毁，查询超过原有 10–30 秒新鲜期再进入时自动重取；搜索条件、结果和歌单当前详情也随组件重建丢失，增加真实服务器请求。
- 预期结果：同一连接会话切换栏目时复用已访问数据和页面状态；只有用户点击刷新、服务器写操作成功、网络上下文变化或会话变化时才按明确范围更新。
- 修复：首页、专辑、艺术家、搜索、收藏和歌单使用 `KeepAlive` 保留实例；对应 TanStack Query 在当前会话保持新鲜并禁用挂载/窗口聚焦重取。各页刷新按钮直接重取当前列表、当前分页或当前详情，不执行全局刷新。设置页不进入该页面缓存；网络设置变化会清查询并轮换页面缓存，断开与忘记账号仍销毁全部会话状态。
- 自动验证：Node.js 22.21.1 下定向 4 文件/21 项及全量 28 文件/146 项通过，`npm run lint` 与 `npm run typecheck` 通过；覆盖卸载重开查询复用、手动刷新、切页后搜索条件/结果保留和请求计数。
- 当前状态：按用户要求未重新构建、未重启或打开新包，真实服务器请求增量待当前测试结束后的统一包复验。

### P12-MI-019：基础交互控件未统一使用 shadcn-vue

- 状态：`RETEST`。
- 平台：Windows 11 x64；代码层面同时影响 Windows 与 macOS 共享 renderer。
- 现象：项目虽已接入 Button、Pagination 和 Sonner，但连接、搜索、歌单、设置、歌词和播放器仍混用原生输入、复选、选择、滑块及手写确认交互。
- 修复：通过官方 CLI 引入并适配 Input、Checkbox、Label、Select、Slider 与 AlertDialog；Sonner 只处理通知。业务导航、实体卡片、队列曲目和虚拟列表行保留语义化按钮，不机械替换业务组合。
- 自动验证：Node.js 22.21.1 下 lint、typecheck、28 文件/147 项测试、生产构建和 Windows 源码 Electron 完整冒烟通过；覆盖设置 Select、表单 Checkbox、AlertDialog、原始与转码 Slider seek。
- 当前状态：未重新生成 Windows 安装包；macOS Intel x64 与 Apple Silicon arm64 未验证。
